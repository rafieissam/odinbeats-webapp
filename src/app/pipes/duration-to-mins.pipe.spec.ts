import { DurationToMinsPipe } from './duration-to-mins.pipe';

describe('DurationToMinsPipe', () => {
  it('create an instance', () => {
    const pipe = new DurationToMinsPipe();
    expect(pipe).toBeTruthy();
  });
});
